import 'package:flutter/material.dart';

class QualityAssessmentScreen extends StatelessWidget {
  const QualityAssessmentScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Quality Assessment Screen'),
    );
  }
}